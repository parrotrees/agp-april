{%- if block.settings.space_around -%}
  <div class=" custom-html   py-5 {% if page.handle  == 'contact-us-test' %} contact-page {% endif %}">
{%- endif -%}
{%- unless block.settings.full_width -%}
  <div class="container-fluid">
{%- endunless -%}
  <div {{ block.shopify_attributes }}>
    <div class="container">
      <div class="row">
        <div class="col-12">
      {%- if block.settings.code != blank -%}
        {{ block.settings.code }}
      {%- else -%}
        {{ 'home_page.onboarding.no_content' | t }}
      {%- endif -%}
          </div>
      </div>
    </div>
  </div>
{%- unless block.settings.full_width -%}
  </div>
{%- endunless -%}
{%- if block.settings.space_around -%}
  </div>
{%- endif -%}